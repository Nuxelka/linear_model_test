# Linear Model Test

Небольшой учебный проект по линейным моделям в машинном обучении.

В этом проекте разбирается обучение линейной модели для задачи регрессии: предсказания стоимости домов (`SalePrice`). Основной акцент сделан не просто на запуске модели, а на том, как подготовка признаков, регуляризация и обработка выбросов влияют на качество предсказаний.

## Что внутри

В проекте используется датасет с признаками домов и целевой переменной `SalePrice`.

Основная работа находится в ноутбуке:

```text
main.ipynb